{
  "name": "kuisto-restaurant-app",
  "version": "1.0.0",
  "description": "Application de restaurant complète avec gestion des commandes",
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run client:dev\" \"npm run admin:dev\"",
    "backend:dev": "cd backend && npm run dev",
    "client:dev": "cd frontend-client && npm start",
    "admin:dev": "cd frontend-admin && npm start",
    "build": "npm run backend:build && npm run client:build && npm run admin:build",
    "backend:build": "cd backend && npm run build",
    "client:build": "cd frontend-client && npm run build",
    "admin:build": "cd frontend-admin && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend-client && npm install && cd ../frontend-admin && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}